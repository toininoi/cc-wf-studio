---
---

CI tooling change only — revert from semantic-release back to Changesets and split the release workflow into a main-triggered Version PR job and a manual production-publish dispatch. No package code changed; no release needed.
